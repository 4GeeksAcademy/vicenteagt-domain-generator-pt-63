  let pronouns = ['the', 'our'];
  let adjs = ['great', 'big'];
  let nouns = ['jogger', 'racoon'];

  for(let indexPronouns = 0; indexPronouns < pronouns.length; indexPronouns++){
    for(let indexAdjs = 0; indexAdjs < adjs.length; indexAdjs++){
      for(let indexNouns = 0; indexNouns < nouns.length; indexNouns++){
console.log(pronouns[indexPronouns]+adjs[indexAdjs]+nouns[indexNouns]+".com")
  }
  }
  }
